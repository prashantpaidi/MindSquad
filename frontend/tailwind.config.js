/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // Tailwind CSS v4 doesn't use the old config format
  // Configuration is now done via @theme directive in CSS
}